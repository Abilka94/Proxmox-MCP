services:
  mcp-proxmox:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../config:/app/config:ro
      - ../data:/app/data
    stdin_open: true
    tty: false
